<div metal:use-macro="sdi_h.macros()['master']">

  <div tal:omit-tag="" metal:fill-slot="head-more">
    <link href="${request.static_url('substanced.sdi:static/dt_bootstrap.css')}" rel="stylesheet" />
    <script type="text/javascript">
      function toggleChecked(status) {
          $("#sdi-contents-table input").each( function() {
             if (!$(this).attr("disabled")) {
               $(this).attr("checked",status);
             }
         })};
    </script>
  </div>

  <div metal:fill-slot="main">
    <div class="container-fluid">
    <form action="@@contents" method="POST">

      <fieldset>

      <h2 tal:condition="not items">No items</h2>

      <div tal:condition="items">

	  <table id="sdi-contents-table" class="table table-striped">
            <thead>
              <th>Name</th>
              <th tal:repeat="header headers">${header}</th>
            </thead>
            <tbody>
	      <tr>
	        <td>
                  <label class="checkbox" for="selectall">
                  <input type="checkbox" onclick="toggleChecked(this.checked)"/>
                  -- all --
                  </label>
                </td>
	        <td tal:repeat="column range(len(headers))">
		</td>
              </tr>
              <tal:loop tal:repeat="choice items">
                <tr tal:condition="choice['viewable']">
                  <td>
                    <label class="checkbox" for="item-${repeat.choice.index}">
                      <i class="${choice['icon']}"> </i>
                      <input tal:attributes="disabled not choice['deletable']"
                             type="checkbox"
                             name="item-modify"
                             value="${choice['name']}"
                             id="item-${repeat.choice.index}"/>
                      <a href="${choice['url']}">${choice['name']}</a>
                    </label>
                  </td>
                  <td tal:repeat="column choice['columns']">${column}</td>
                </tr>
              </tal:loop>
            </tbody>
          </table> 

      </div>

      <div class="clearfix">
      </div>

      <input type="hidden" name="csrf_token" 
             value="${request.session.get_csrf_token()}"/>

      <div class="form-actions btn-toolbar">
        <tal:block tal:condition="'tocopy' in request.session">
          <button id="copy_finish"
                  name="form.copy_finish"
                  type="submit"
                  class="btn btn-primary"
                  value="copy_finish">
            Copy here
          </button>

          <button
              id="cancel"
              name="form.copy_finish"
              type="submit"
              class="btn btn-danger"
              value="cancel">
            Cancel
          </button>
        </tal:block>
        <tal:block tal:condition="'tomove' in request.session">
          <button id="move_finish"
                  name="form.move_finish"
                  type="submit"
                  class="btn btn-primary"
                  value="move_finish">
            Move here
          </button>

          <button
              id="cancel"
              name="form.move_finish"
              type="submit"
              class="btn btn-danger"
              value="cancel">
            Cancel
          </button>
        </tal:block>

        <tal:block tal:condition="'tocopy' not in request.session and 'tomove' not in request.session">
        <div class="btn-group">
          <button id="rename"
                  name="form.rename"
                  type="submit"
                  class="btn"
                  value="rename">
            Rename
          </button>
          <button id="copy"
                  name="form.copy"
                  type="submit"
                  class="btn"
                  value="copy">
             Copy
          </button>
          <button id="move"
                  name="form.move"
                  type="submit"
                  class="btn"
                  value="move">
            Move
          </button>
          <button id="duplicate"
                  name="form.duplicate"
                  type="submit"
                  class="btn"
                  value="duplicate">
            Duplicate
          </button>
        </div>

        <div class="btn-group">
          <button
              id="delete"
              name="form.delete"
              type="submit"
              class="btn btn-danger"
              value="delete">
            Delete
          </button>
        </div>
        <div class="btn-group pull-right" tal:condition="addables">
          <a class="btn btn-primary" 
             href="${request.mgmt_path(context, '@@add')}"><i 
             class="icon-plus-sign icon-white"></i>Add</a>
          <a class="btn btn-primary dropdown-toggle" 
              data-toggle="dropdown" href="#"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li tal:repeat="addable addables">
               <a href="${addable['url']}"><i 
              class="${addable['icon']}"></i> ${addable['type_name']}</a></li>
          </ul>
        </div>
        </tal:block>

      </div>
      </fieldset>
    </form>

  </div>
  </div>


  <div tal:omit-tag="" metal:fill-slot="tail-more">
    <script src="${request.static_url('substanced.sdi:static/dt_bootstrap.js')}" type="text/javascript"></script>
  </div>

</div>
